mutation RegisterAccount($input: RegisterInput!) {
  register(input: $input) {
    user {
      avatarUrl
      isAdmin
      isVerified
      name
      username
      hasPassword
      id
    }
  }
}
